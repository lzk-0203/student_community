# 端口号
server.port=8081
# mysql配置
spring.datasource.url=jdbc:mysql://localhost:3306/stu_community?serverTimezone=UTC&useSSL=false
spring.datasource.username=root
spring.datasource.password=010203

# Tomcat
server.tomcat.uri-encoding=utf-8

# Redis服务器地址
spring.redis.host=127.0.0.1
# Redis服务器连接端口
spring.redis.port=6379
# Redis服务器连接密码（默认为空）
spring.redis.password=

#添加并配置第三方数据库Druid
spring.datasource.type = com.alibaba.druid.pool.DruidDataSource
spring.datasource.initialSize=20
spring.datasource.minIdle=10
spring.datasource.maxActive=100

# mybatis驼峰字段自动匹配
mybatis.configuration.map-underscore-to-camel-case=true
# mybatis generator配置
mybatis.type-aliases-package=com.sdut.community.model
mybatis.mapper-locations=classpath:mapper/*.xml

# PageHelper 分页管理, 没咋用到
pagehelper.helper-dialect=mysql
pagehelper.reasonable=true
pagehelper.support-methods-arguments=true
pagehelper.params=count=countSql

# 配置字段
github.client.id=ceb23fb5d2e932187320
github.client.secret=744bfa7e37833dedf73c10f5bebf60342bf9d96a
github.redirect.uri=http://localhost:8081/callback